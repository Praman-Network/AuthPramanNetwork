{
  "name": "pramanauth-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "packages/*",
    "apps/*",
    "server/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "hardhat:node": "npx hardhat --config hardhat.config.cjs node"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "~6.0.2"
  }
}
